FROM node:18-alpine

# Sharp 빌드를 위한 필수 패키지만 설치
RUN apk add --no-cache vips-dev python3 make g++

WORKDIR /app

COPY package*.json ./
RUN npm ci --only=production && npm rebuild sharp

COPY . .

EXPOSE 5001

CMD ["node", "server.js"]